/** A screen-space rectangle in window coordinates (from View.measureInWindow).
 *  Used to drive the viewer's shared-element open/close from a grid tile. */
export type Rect = { x: number; y: number; width: number; height: number };
